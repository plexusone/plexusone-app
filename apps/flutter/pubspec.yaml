name: plexusone_mobile
description: PlexusOne Mobile Companion - Monitor and control AI agents

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  web_socket_channel: ^3.0.0
  provider: ^6.1.2
  shared_preferences: ^2.3.0
  xterm: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

dependency_overrides:
  win32: ^5.5.0

flutter:
  uses-material-design: true
